import { action, observable, computed } from 'mobx';


class MainStore  {
// @ is a decoratores we need to install an babel plugin
   //@observable test = 'hello from store';
 
   
   // @action getData() {
   //  // this.didUserAcceptAllCookies = accept;
   // }
 
  
 
   // @computed get arabicCountryName(): string|undefined {
   //   if(!this.countryCode) return undefined;
   //   const country =   this.countriesArabic.find((item:ICountriesArabicNames) => this.countryCode?.toLowerCase() === item.alpha2);    
   //   // eslint-disable-next-line consistent-return
   //   return country?.name;
   // }
 
  
 
  
 }
  
 export default MainStore;
  